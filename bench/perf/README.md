Benchmarking performances of Web standard JS frameworks.

To run this benchmark, go to the project root directory and run:
```sh
# Run benchmark in Bun
bun task bench perf/index

# Run benchmark in Node
bun task bench --node perf/index
```
